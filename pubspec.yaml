name: payment_collect_02
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  qr_flutter: ^4.1.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.1.0
  path_provider: ^2.1.3  # ✅ Updated to fix V1 embedding issue
  intl: ^0.19.0
  csv: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# ✅ Optional: Force override to use safe plugin version
dependency_overrides:
  path_provider_android: 2.2.4

flutter:
  uses-material-design: true
